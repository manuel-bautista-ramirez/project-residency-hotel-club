<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.5s ease-out forwards;
  }

  /* Custom active state for period buttons */
  .period-option.active {
    border-color: #3B82F6;
    background-color: #EFF6FF;
    box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.1), 0 2px 4px -1px rgba(59, 130, 246, 0.06);
    transform: translateY(-2px);
  }
</style>
<div class="animate-fade-in">

  <!-- Navigation Header & Filtros Sticky -->
  <div class="bg-white shadow-sm border-b border-slate-200 sticky top-0 z-30 pt-10">
    <div class="w-full px-3 sm:px-4 lg:px-8">
      <div class="mt-2 w-full border-t border-slate-100 pt-3 pb-2">
        {{> storeNavButtons activePage="reports" user=user}}
      </div>
    </div>
  </div>

  <!-- Contenido principal -->
  <div class="w-full px-3 sm:px-4 lg:px-8">

    <!-- Header del Panel -->
    <div class="mb-6 text-center pt-2">
      <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent mb-1">
        Reportes y Estadísticas
      </h1>
    </div>

    <div class="bg-white rounded-2xl shadow-md p-6 border border-gray-100">

      <!-- Selector de Período -->
      <div class="mb-8">
        <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 flex items-center">
          <span
            class="p-1.5 bg-blue-100 text-blue-600 rounded-lg mr-2 text-sm shadow-sm group-hover:scale-110 transition-transform">
            <i class="fas fa-calendar-alt"></i>
          </span>
          Período del Reporte
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-4 gap-3 md:gap-4">
          <button
            class="period-option border-2 border-slate-200 bg-white rounded-xl p-4 text-center cursor-pointer transition-all duration-200 hover:border-blue-300 hover:bg-slate-50 active"
            data-period="diario">
            <div class="bg-blue-100 text-blue-600 w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-calendar-day text-lg"></i>
            </div>
            <h3 class="font-bold text-gray-700 text-sm">Diario</h3>
          </button>

          <button
            class="period-option border-2 border-slate-200 bg-white rounded-xl p-4 text-center cursor-pointer transition-all duration-200 hover:border-green-300 hover:bg-slate-50"
            data-period="semanal">
            <div
              class="bg-green-100 text-green-600 w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-calendar-week text-lg"></i>
            </div>
            <h3 class="font-bold text-gray-700 text-sm">Semanal</h3>
          </button>

          <button
            class="period-option border-2 border-slate-200 bg-white rounded-xl p-4 text-center cursor-pointer transition-all duration-200 hover:border-purple-300 hover:bg-slate-50"
            data-period="quincenal">
            <div
              class="bg-purple-100 text-purple-600 w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-calendar-alt text-lg"></i>
            </div>
            <h3 class="font-bold text-gray-700 text-sm">Quincenal</h3>
          </button>

          <button
            class="period-option border-2 border-slate-200 bg-white rounded-xl p-4 text-center cursor-pointer transition-all duration-200 hover:border-orange-300 hover:bg-slate-50"
            data-period="mensual">
            <div
              class="bg-orange-100 text-orange-600 w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-calendar text-lg"></i>
            </div>
            <h3 class="font-bold text-gray-700 text-sm">Mensual</h3>
          </button>
        </div>
      </div>

      <!-- Filtros y Fechas -->
      <div class="mb-6 bg-slate-50 p-6 rounded-xl border border-slate-200">
        <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 flex items-center">
          <span class="p-1.5 bg-gray-200 text-gray-600 rounded-lg mr-2 text-sm shadow-sm">
            <i class="fas fa-filter"></i>
          </span>
          Filtros y Fechas
        </h2>

        <div id="selector-diario" class="date-selector mb-4">
          <label class="block text-sm font-bold text-gray-700 mb-2">Selecciona el día:</label>
          <input type="date" id="fecha-diaria"
            class="border border-gray-300 rounded-lg p-3 w-full md:w-64 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-shadow">
        </div>

        <div id="selector-semanal" class="date-selector hidden mb-4">
          <label class="block text-sm font-bold text-gray-700 mb-2">Selecciona la semana:</label>
          <input type="week" id="fecha-semanal"
            class="border border-gray-300 rounded-lg p-3 w-full md:w-64 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-shadow">
        </div>

        <div id="selector-quincenal" class="date-selector hidden mb-4">
          <label class="block text-sm font-bold text-gray-700 mb-2">Selecciona la quincena:</label>
          <div class="flex flex-col sm:flex-row gap-4 mb-4">
            <label
              class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg bg-white cursor-pointer hover:border-blue-400">
              <input type="radio" id="primera-quincena" name="quincena" value="primera" checked
                class="form-radio h-4 w-4 text-blue-600 focus:ring-blue-500">
              <span class="text-sm font-medium text-gray-700">Primera quincena (1-15)</span>
            </label>
            <label
              class="flex items-center space-x-3 p-3 border border-gray-200 rounded-lg bg-white cursor-pointer hover:border-blue-400">
              <input type="radio" id="segunda-quincena" name="quincena" value="segunda"
                class="form-radio h-4 w-4 text-blue-600 focus:ring-blue-500">
              <span class="text-sm font-medium text-gray-700">Segunda quincena (16-fin)</span>
            </label>
          </div>
          <input type="month" id="fecha-quincenal"
            class="border border-gray-300 rounded-lg p-3 w-full md:w-64 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-shadow">
        </div>

        <div id="selector-mensual" class="date-selector hidden mb-4">
          <label class="block text-sm font-bold text-gray-700 mb-2">Selecciona el mes:</label>
          <input type="month" id="fecha-mensual"
            class="border border-gray-300 rounded-lg p-3 w-full md:w-64 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-shadow">
        </div>

        <!-- Botones de Acción -->
        <div class="mt-6 pt-4 border-t border-slate-200">
          <div class="flex flex-col sm:flex-row gap-3 sm:gap-4">
            <button id="generar-reporte-btn"
              class="bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-6 py-3 rounded-lg transition-all duration-300 flex items-center justify-center text-sm font-bold shadow-md hover:shadow-lg hover:-translate-y-0.5">
              <i class="fas fa-chart-bar mr-2"></i>Generar Reporte
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Vista previa del reporte -->
    <div id="reporte-container"
      class="bg-gray-50 rounded-2xl md:p-8 mt-6 mb-6 shadow-inner border border-gray-100 min-h-[200px] flex flex-col justify-center">
      <div class="flex flex-col justify-center items-center h-full text-center">
        <div class="bg-white p-4 rounded-full shadow-sm mb-3">
          <i class="fas fa-chart-pie text-gray-300 text-4xl"></i>
        </div>
        <p class="text-gray-500 font-medium">Selecciona un período y genera la vista previa del reporte</p>
      </div>
    </div>

    <!-- Opciones de envío -->
    <div id="envio-opciones"
      class="bg-white border border-gray-100 rounded-2xl shadow-md p-6 mb-6 hidden animate-fade-in">
      <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-6 flex items-center">
        <span class="p-1.5 bg-indigo-100 text-indigo-600 rounded-lg mr-2 text-sm shadow-sm">
          <i class="fas fa-paper-plane"></i>
        </span>
        Enviar Reporte
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-8">
        <!-- Envío por correo -->
        <div class="border border-gray-200 rounded-xl p-6 hover:shadow-md transition-shadow">
          <h3 class="font-bold text-gray-800 mb-4 flex items-center">
            <i class="fas fa-envelope mr-2 text-blue-500 bg-blue-50 p-1.5 rounded-lg"></i>Envío por Correo
          </h3>
          <div class="space-y-4">
            <div>
              <label class="block text-xs font-bold text-gray-500 mb-1 uppercase">Destinatario</label>
              <input type="email" id="email-destinatario" placeholder="correo@ejemplo.com"
                class="border border-gray-300 rounded-lg p-2.5 w-full focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
            </div>
            <div>
              <label class="block text-xs font-bold text-gray-500 mb-1 uppercase">Asunto</label>
              <input type="text" id="email-asunto" placeholder="Asunto del correo"
                class="border border-gray-300 rounded-lg p-2.5 w-full focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
            </div>
            <button id="enviar-email-btn"
              class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-3 rounded-lg w-full transition-all duration-300 text-sm font-bold shadow-md hover:shadow-lg hover:-translate-y-0.5">
              <i class="fas fa-envelope mr-2"></i> Enviar por Correo
            </button>
          </div>
        </div>

        <!-- Envío por WhatsApp -->
        <div class="border border-gray-200 rounded-xl p-6 hover:shadow-md transition-shadow">
          <h3 class="font-bold text-gray-800 mb-4 flex items-center">
            <i class="fab fa-whatsapp mr-2 text-green-500 bg-green-50 p-1.5 rounded-lg"></i>Envío por WhatsApp
          </h3>
          <div class="space-y-4">
            <div>
              <label class="block text-xs font-bold text-gray-500 mb-1 uppercase">Número de celular</label>
              <input type="tel" id="whatsapp-telefono" placeholder="10 dígitos sin espacios"
                class="border border-gray-300 rounded-lg p-2.5 w-full focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500 text-sm">
            </div>
            <button id="enviar-whatsapp-btn"
              class="bg-green-600 hover:bg-green-700 text-white px-4 py-3 rounded-lg w-full transition-all duration-300 text-sm font-bold shadow-md hover:shadow-lg hover:-translate-y-0.5">
              <i class="fab fa-whatsapp mr-2"></i> Enviar por WhatsApp
            </button>
          </div>
        </div>
      </div>
    </div>

  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
      let reporteActual = null;
      let periodoSeleccionado = 'diario';

      // Funcionalidad para cambiar entre períodos
      document.querySelectorAll('.period-option').forEach(option => {
        option.addEventListener('click', function () {
          document.querySelectorAll('.period-option').forEach(opt => {
            opt.classList.remove('active');
          });
          this.classList.add('active');
          periodoSeleccionado = this.getAttribute('data-period');
          document.querySelectorAll('.date-selector').forEach(selector => selector.classList.add('hidden'));
          document.getElementById(`selector-${periodoSeleccionado}`).classList.remove('hidden');
        });
      });

      // Establecer fechas por defecto
      const today = new Date();
      const year = today.getFullYear();
      const month = (today.getMonth() + 1).toString().padStart(2, '0');
      const day = today.getDate().toString().padStart(2, '0');
      document.getElementById('fecha-diaria').value = `${year}-${month}-${day}`;
      document.getElementById('fecha-mensual').value = `${year}-${month}`;
      document.getElementById('fecha-quincenal').value = `${year}-${month}`;
      const firstDayOfYear = new Date(year, 0, 1);
      const pastDaysOfYear = (today - firstDayOfYear) / 86400000;
      const week = Math.ceil((pastDaysOfYear + firstDayOfYear.getDay() + 1) / 7);
      document.getElementById('fecha-semanal').value = `${year}-W${week.toString().padStart(2, '0')}`;

      // --- Lógica para generar y enviar reportes ---
      const reporteContainer = document.getElementById('reporte-container');
      const envioOpciones = document.getElementById('envio-opciones');

      function calcularFechas() {
        let fechaInicio, fechaFin;
        if (periodoSeleccionado === 'diario') {
          const fecha = document.getElementById('fecha-diaria').value;
          if (!fecha) { mostrarNotificacion('Por favor selecciona una fecha', 'error'); return null; }
          fechaInicio = fecha;
          fechaFin = fecha;
        } else if (periodoSeleccionado === 'semanal') {
          const semana = document.getElementById('fecha-semanal').value;
          if (!semana) { mostrarNotificacion('Por favor selecciona una semana', 'error'); return null; }
          const [year, weekNum] = semana.split('-W');
          const firstDayOfYear = new Date(year, 0, 1);
          const daysOffset = (parseInt(weekNum) - 1) * 7;
          const weekStart = new Date(firstDayOfYear.getTime() + daysOffset * 24 * 60 * 60 * 1000);
          const weekEnd = new Date(weekStart.getTime() + 6 * 24 * 60 * 60 * 1000);
          fechaInicio = weekStart.toISOString().split('T')[0];
          fechaFin = weekEnd.toISOString().split('T')[0];
        } else if (periodoSeleccionado === 'quincenal') {
          const mes = document.getElementById('fecha-quincenal').value;
          const quincena = document.querySelector('input[name="quincena"]:checked').value;
          if (!mes) { mostrarNotificacion('Por favor selecciona un mes', 'error'); return null; }
          const [year, month] = mes.split('-');
          if (quincena === 'primera') {
            fechaInicio = `${year}-${month}-01`;
            fechaFin = `${year}-${month}-15`;
          } else {
            fechaInicio = `${year}-${month}-16`;
            const lastDay = new Date(year, month, 0).getDate();
            fechaFin = `${year}-${month}-${lastDay}`;
          }
        } else if (periodoSeleccionado === 'mensual') {
          const mes = document.getElementById('fecha-mensual').value;
          if (!mes) { mostrarNotificacion('Por favor selecciona un mes', 'error'); return null; }
          const [year, month] = mes.split('-');
          fechaInicio = `${year}-${month}-01`;
          const lastDay = new Date(year, month, 0).getDate();
          fechaFin = `${year}-${month}-${lastDay}`;
        }
        return { fechaInicio, fechaFin };
      }

      async function generarReporte() {
        const fechas = calcularFechas();
        if (!fechas) return;
        const { fechaInicio, fechaFin } = fechas;

        reporteContainer.innerHTML = `<div class="flex justify-center items-center h-40"><i class="fas fa-spinner fa-spin text-3xl text-blue-500"></i></div>`;
        mostrarNotificacion('Generando reporte...', 'info');

        try {
          const params = new URLSearchParams({ fechaInicio, fechaFin });
          const response = await fetch(`/store/api/reports?${params}`);
          const data = await response.json();
          if (!response.ok) throw new Error(data.error || 'Error al generar el reporte');
          reporteActual = data.reporte;
          mostrarReporte(reporteActual);

          // Mostrar u ocultar opciones de envío según si hay datos
          if (reporteActual && reporteActual.datos && reporteActual.datos.length > 0) {
            envioOpciones.classList.remove('hidden');
            mostrarNotificacion('Reporte generado correctamente', 'success');
          } else {
            envioOpciones.classList.add('hidden');
            mostrarNotificacion('No hay datos disponibles para el período seleccionado', 'warning');
          }
        } catch (error) {
          reporteContainer.innerHTML = `<p class="text-red-500 text-center font-bold">Error: ${error.message}</p>`;
          mostrarNotificacion(`Error generando el reporte: ${error.message}`, 'error');
          envioOpciones.classList.add('hidden');
        }
      }

      function mostrarReporte(reporte) {
        const formatCurrency = (value) => new Intl.NumberFormat('es-MX', { style: 'currency', currency: 'MXN' }).format(value);
        let contenidoHTML;

        // Estilos coincidentes para la tabla dinámica
        if (reporte.datos.length === 0) {
          contenidoHTML = `
                <div class="text-center py-12">
                    <div class="bg-gray-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4">
                       <i class="fas fa-info-circle text-3xl text-gray-400"></i>
                    </div>
                    <h2 class="mt-4 text-xl font-bold text-gray-700">No se encontraron ventas</h2>
                    <p class="mt-2 text-sm text-gray-500">No hay datos de ventas para el período seleccionado.</p>
                </div>`;
        } else {
          contenidoHTML = `
                <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-4 mb-6">
                   <h2 class="text-lg font-bold text-gray-800 mb-4 border-b border-gray-100 pb-2">Resumen Financiero</h2>
                   <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                       <div class="bg-gradient-to-br from-blue-50 to-blue-100 p-4 rounded-xl border border-blue-200">
                          <h3 class="text-xs font-bold text-blue-600 uppercase mb-1">Total Ventas</h3>
                          <p class="text-2xl font-bold text-blue-900">${reporte.estadisticas.totalVentas}</p>
                       </div>
                       <div class="bg-gradient-to-br from-green-50 to-green-100 p-4 rounded-xl border border-green-200">
                          <h3 class="text-xs font-bold text-green-600 uppercase mb-1">Ingreso Total</h3>
                          <p class="text-2xl font-bold text-green-900">${formatCurrency(reporte.estadisticas.totalIngresos)}</p>
                       </div>
                       <div class="bg-gradient-to-br from-purple-50 to-purple-100 p-4 rounded-xl border border-purple-200">
                          <h3 class="text-xs font-bold text-purple-600 uppercase mb-1">Venta Promedio</h3>
                          <p class="text-2xl font-bold text-purple-900">${formatCurrency(reporte.estadisticas.promedioVenta)}</p>
                       </div>
                   </div>
                </div>

                <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
                    <div class="px-4 py-3 bg-gradient-to-r from-gray-50 to-gray-100 border-b border-gray-200">
                       <h3 class="font-bold text-gray-800">Detalle de Ventas</h3>
                    </div>

                    <!-- Vista Móvil (Cards) -->
                    <div class="md:hidden p-4 space-y-3 bg-slate-50">
                        ${reporte.datos.map(venta => `
                        <div class="bg-white p-4 rounded-xl shadow-sm border border-gray-100">
                            <div class="flex justify-between items-center mb-2 border-b border-gray-50 pb-2">
                                <span class="text-sm font-bold text-gray-900">#${venta.id}</span>
                                <span class="text-lg font-bold text-blue-600">${formatCurrency(venta.total)}</span>
                            </div>
                            <div class="mb-3 space-y-1">
                                <p class="text-sm font-bold text-gray-800 flex items-center">
                                    <i class="fas fa-user text-gray-400 mr-2 text-xs"></i>
                                    ${venta.nombre_cliente}
                                </p>
                                <p class="text-xs text-gray-500 flex items-center">
                                    <i class="far fa-clock text-gray-400 mr-2 text-xs"></i>
                                    ${new Date(venta.fecha_venta).toLocaleDateString('es-MX')} ${new Date(venta.fecha_venta).toLocaleTimeString('es-MX', { hour: '2-digit', minute: '2-digit' })}
                                </p>
                            </div>
                            <div class="flex justify-between items-center pt-2">
                                <span class="inline-flex items-center px-2.5 py-1 rounded-md text-xs font-bold capitalize
                                    ${venta.tipo_pago === 'efectivo' ? 'bg-green-100 text-green-700' :
              venta.tipo_pago === 'tarjeta' ? 'bg-purple-100 text-purple-700' :
                'bg-blue-100 text-blue-700'}">
                                    <i class="${venta.tipo_pago === 'efectivo' ? 'fas fa-money-bill-wave' : venta.tipo_pago === 'tarjeta' ? 'fas fa-credit-card' : 'fas fa-money-check'} mr-1.5" style="font-size: 0.7rem;"></i>
                                    ${venta.tipo_pago}
                                </span>
                            </div>
                        </div>`).join('')}
                    </div>

                    <!-- Vista Escritorio (Tabla) -->
                    <div class="hidden md:block overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gradient-to-r from-slate-50 to-slate-100">
                                <tr>
                                    <th class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">ID</th>
                                    <th class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Cliente</th>
                                    <th class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Fecha</th>
                                    <th class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Tipo Pago</th>
                                    <th class="px-4 py-3 text-right text-xs font-bold text-gray-700 uppercase tracking-wider">Total</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-200 bg-white">
                                ${reporte.datos.map(venta => `
                                <tr class="hover:bg-slate-50 transition-colors">
                                    <td class="px-4 py-3 text-sm font-bold text-gray-900">#${venta.id}</td>
                                    <td class="px-4 py-3 text-sm text-gray-700 font-medium">${venta.nombre_cliente}</td>
                                    <td class="px-4 py-3 text-sm text-gray-500">${new Date(venta.fecha_venta).toLocaleDateString('es-MX')} ${new Date(venta.fecha_venta).toLocaleTimeString('es-MX', { hour: '2-digit', minute: '2-digit' })}</td>
                                    <td class="px-4 py-3 text-sm">
                                        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-bold capitalize
                                            ${venta.tipo_pago === 'efectivo' ? 'bg-green-100 text-green-800' :
                    venta.tipo_pago === 'tarjeta' ? 'bg-purple-100 text-purple-800' :
                      'bg-blue-100 text-blue-800'}">
                                            ${venta.tipo_pago}
                                        </span>
                                    </td>
                                    <td class="px-4 py-3 text-sm text-right font-bold text-blue-600">${formatCurrency(venta.total)}</td>
                                </tr>`).join('')}
                            </tbody>
                        </table>
                    </div>
                </div>`;
        }

        reporteContainer.innerHTML = contenidoHTML;
        reporteContainer.classList.remove('flex', 'flex-col', 'justify-center'); // Clear alignment classes if needed
      }

      async function enviarEmail() {
        if (!reporteActual) { mostrarNotificacion('Primero genera un reporte', 'error'); return; }
        const destinatario = document.getElementById('email-destinatario').value;
        const asunto = document.getElementById('email-asunto').value;
        if (!destinatario) { mostrarNotificacion('Ingresa un correo de destinatario', 'error'); return; }

        const btn = document.getElementById('enviar-email-btn');
        const originalContent = btn.innerHTML;
        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> Enviando...';

        mostrarNotificacion('Iniciando envío de correo...', 'info');
        try {
          const range = calcularFechas();
          const response = await fetch('/store/api/reports/email', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ ...range, periodo: periodoSeleccionado, destinatario, asunto })
          });
          const result = await response.json();
          if (!response.ok) throw new Error(result.error);
          mostrarNotificacion(result.message, 'success');
        } catch (error) {
          mostrarNotificacion(`Error al enviar: ${error.message}`, 'error');
        } finally {
          btn.disabled = false;
          btn.innerHTML = originalContent;
        }
      }

      async function enviarWhatsApp() {
        if (!reporteActual) { mostrarNotificacion('Primero genera un reporte', 'error'); return; }
        const telefono = document.getElementById('whatsapp-telefono').value;
        if (!telefono) { mostrarNotificacion('Ingresa un número de teléfono', 'error'); return; }

        const btn = document.getElementById('enviar-whatsapp-btn');
        const originalContent = btn.innerHTML;
        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> Enviando...';

        mostrarNotificacion('Iniciando envío de WhatsApp...', 'info');
        try {
          const range = calcularFechas();
          const response = await fetch('/store/api/reports/whatsapp', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ ...range, periodo: periodoSeleccionado, telefono })
          });
          const result = await response.json();
          if (!response.ok) throw new Error(result.error);
          mostrarNotificacion(result.message, 'success');
        } catch (error) {
          mostrarNotificacion(`Error al enviar: ${error.message}`, 'error');
        } finally {
          btn.disabled = false;
          btn.innerHTML = originalContent;
        }
      }

      function mostrarNotificacion(mensaje, tipo = 'success') {
        if (typeof window.showNotification === 'function') {
          window.showNotification(mensaje, tipo);
        } else {
          alert(mensaje);
        }
      }

      document.getElementById('generar-reporte-btn').addEventListener('click', generarReporte);
      document.getElementById('enviar-email-btn').addEventListener('click', enviarEmail);
      document.getElementById('enviar-whatsapp-btn').addEventListener('click', enviarWhatsApp);
    });
  </script>
  <script src="/js/ModuleRooms/js/reportsInputValidation.js"></script>
</div>
