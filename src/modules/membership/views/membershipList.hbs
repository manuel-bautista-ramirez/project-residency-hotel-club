<!-- Navbar-->
{{>navbar}}

<div class="container mx-auto px-4 py-6 mt-20">
    <!-- Header con título y botones de acción -->
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8">
        <div>
            <h1 class="text-3xl font-bold text-gray-800">{{title}}</h1>
        </div>

        <div class="flex flex-wrap gap-3 mt-4 md:mt-0">
            <a href="/memberships/createMembership"
                class="bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white px-5 py-2.5 rounded-xl font-medium flex items-center transition-all duration-300 transform hover:-translate-y-0.5 shadow-lg hover:shadow-xl">
                <i class="fas fa-plus-circle mr-2"></i> Nueva Membresía
            </a>
        </div>
    </div>

    <!-- Sección de estadísticas -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <div
            class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg p-6 border-l-0 border-t-4 border-t-blue-400 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-24 h-24 -mr-4 -mt-4 bg-blue-100 rounded-full opacity-30"></div>
            <div class="flex justify-between items-center relative z-10">
                <div class="flex-1">
                    <p class="text-gray-700 text-sm font-semibold uppercase tracking-wide">Total Membresías</p>
                    <h3 class="text-3xl font-bold text-gray-800 mt-2" id="total-membresias">{{estadisticas.total}}
                    </h3>
                    <div class="flex items-center mt-3">
                        <span class="bg-blue-100 text-blue-700 text-xs font-medium px-3 py-1.5 rounded-full">
                            <i class="fas fa-database mr-1.5"></i>Total registradas
                        </span>
                    </div>
                </div>
                <div class="bg-blue-100 p-3.5 rounded-xl ml-4 flex-shrink-0">
                    <i class="fas fa-id-card-alt text-blue-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div
            class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg p-6 border-l-0 border-t-4 border-t-emerald-400 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-24 h-24 -mr-4 -mt-4 bg-emerald-100 rounded-full opacity-30"></div>
            <div class="flex justify-between items-center relative z-10">
                <div class="flex-1">
                    <p class="text-gray-700 text-sm font-semibold uppercase tracking-wide">Membresías Activas</p>
                    <h3 class="text-3xl font-bold text-gray-800 mt-2" id="activas-membresias">
                        {{estadisticas.activas}}</h3>
                    <div class="flex items-center mt-3">
                        <span class="bg-emerald-100 text-emerald-700 text-xs font-medium px-3 py-1.5 rounded-full">
                            <i class="fas fa-check-circle mr-1.5"></i>En vigor
                        </span>
                    </div>
                </div>
                <div class="bg-emerald-100 p-3.5 rounded-xl ml-4 flex-shrink-0">
                    <i class="fas fa-check-circle text-emerald-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div
            class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg p-6 border-l-0 border-t-4 border-t-amber-400 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-24 h-24 -mr-4 -mt-4 bg-amber-100 rounded-full opacity-30"></div>
            <div class="flex justify-between items-center relative z-10">
                <div class="flex-1">
                    <p class="text-gray-700 text-sm font-semibold uppercase tracking-wide">Por Vencer</p>
                    <h3 class="text-3xl font-bold text-gray-800 mt-2" id="por-vencer-membresias">
                        {{estadisticas.por_vencer}}</h3>
                    <div class="flex items-center mt-3">
                        <span class="bg-amber-100 text-amber-700 text-xs font-medium px-3 py-1.5 rounded-full">
                            <i class="fas fa-clock mr-1.5"></i>Próximos 7 días
                        </span>
                    </div>
                </div>
                <div class="bg-amber-100 p-3.5 rounded-xl ml-4 flex-shrink-0">
                    <i class="fas fa-exclamation-triangle text-amber-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div
            class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg p-6 border-l-0 border-t-4 border-t-red-400 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-24 h-24 -mr-4 -mt-4 bg-red-100 rounded-full opacity-30"></div>
            <div class="flex justify-between items-center relative z-10">
                <div class="flex-1">
                    <p class="text-gray-700 text-sm font-semibold uppercase tracking-wide">Vencidas</p>
                    <h3 class="text-3xl font-bold text-gray-800 mt-2" id="vencidas-membresias">
                        {{estadisticas.vencidas}}</h3>
                    <div class="flex items-center mt-3">
                        <span class="bg-red-100 text-red-700 text-xs font-medium px-3 py-1.5 rounded-full">
                            <i class="fas fa-clock mr-1.5"></i>Necesitan renovación
                        </span>
                    </div>
                </div>
                <div class="bg-red-100 p-3.5 rounded-xl ml-4 flex-shrink-0">
                    <i class="fas fa-times-circle text-red-600 text-xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Filtros y Búsqueda Mejorados -->
    <div class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg p-6 mb-8 border border-gray-200">
        <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-5">
            <div class="flex-1">
                <label class="block text-gray-700 text-sm font-semibold mb-2">🔍 Búsqueda rápida</label>
                <div class="relative">
                    <input type="text" id="searchInput" placeholder="Buscar por nombre, teléfono o email..."
                        class="w-full pl-12 pr-4 py-3.5 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-white">
                    <i class="fas fa-search absolute left-4 top-3.5 text-gray-500"></i>
                </div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2">📊 Filtro de Estado</label>
                    <select id="statusFilter"
                        class="w-full px-4 py-3.5 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-white">
                        <option value="">Todos los Estados</option>
                        <option value="active">Activa</option>
                        <option value="expiring">Por Vencer</option>
                        <option value="expired">Vencida</option>
                        <option value="scheduled">Programada</option>
                    </select>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2">🏷️ Filtro por Tipo</label>
                    <select id="typeFilter"
                        class="w-full px-4 py-3.5 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-white">
                        <option value="">Todos los Tipos</option>
                        {{#each membershipTypes}}
                        <option value="{{this.id_tipo_membresia}}">{{this.nombre}}</option>
                        {{/each}}
                    </select>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2">🔄 Ordenar por</label>
                    <select id="sortBy"
                        class="w-full px-4 py-3.5 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-white">
                        <option value="expiry">Próximas a vencer</option>
                        <option value="recent">Más recientes</option>
                        <option value="name">Orden alfabético</option>
                    </select>
                </div>
            </div>
        </div>
    </div>

    <!-- Tabla de membresías mejorada -->
    <div class="bg-white rounded-2xl shadow-lg overflow-hidden border border-gray-200">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th scope="col"
                            class="px-6 py-4 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                            <div class="flex items-center">
                                <i class="fas fa-user-circle mr-2 text-gray-500"></i> Miembro
                            </div>
                        </th>
                        <th scope="col"
                            class="px-4 py-4 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                            <div class="flex items-center">
                                <i class="fas fa-tag mr-2 text-gray-500"></i> Tipo
                            </div>
                        </th>
                        <th scope="col"
                            class="px-4 py-4 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                            <div class="flex items-center">
                                <i class="fas fa-calendar-alt mr-2 text-gray-500"></i> Periodo de Vigencia
                            </div>
                        </th>
                        <th scope="col"
                            class="px-4 py-4 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                            <div class="flex items-center">
                                <i class="fas fa-info-circle mr-2 text-gray-500"></i> Estado
                            </div>
                        </th>
                        <th scope="col"
                            class="px-4 py-4 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">
                            <div class="flex items-center">
                                <i class="fas fa-cog mr-2 text-gray-500"></i> Acciones
                            </div>
                        </th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200" id="membershipsTableBody">
                    {{#each memberships}}
                    <tr class="table-row bg-white hover:bg-gray-50 transition-all duration-200 group membership-row"
                        data-days-until-expiry="{{this.daysUntilExpiry}}">
                        <td class="px-6 py-4 whitespace-nowrap">
                            <div class="flex items-center">
                                <div class="flex-shrink-0 h-10 w-10">
                                    <div
                                        class="h-10 w-10 rounded-full bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center shadow-sm group-hover:shadow-md transition-shadow duration-200 border border-gray-200">
                                        <span class="text-gray-600 font-bold"
                                            data-initial="{{this.nombre_completo}}"></span>
                                    </div>
                                </div>
                                <div class="ml-3">
                                    <div
                                        class="text-sm font-semibold text-gray-900 group-hover:text-blue-600 transition-colors duration-200">
                                        {{this.nombre_completo}}</div>
                                    <div class="text-xs text-gray-500 flex items-center mt-1">
                                        <i class="fas fa-phone-alt mr-1 text-gray-500"></i>{{this.telefono}}
                                    </div>
                                </div>
                            </div>
                        </td>
                        <td class="px-4 py-4 whitespace-nowrap">
                            <div class="text-sm text-gray-900">
                                {{#if this.isFamily}}
                                <span
                                    class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-purple-100 text-purple-800 border border-purple-200">
                                    <i class="fas fa-users mr-1 text-purple-600"></i> Familiar
                                </span>
                                {{else}}
                                <span
                                    class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800 border border-blue-200">
                                    <i class="fas fa-user mr-1 text-blue-600"></i> {{this.tipo_membresia}}
                                </span>
                                {{/if}}
                            </div>
                        </td>
                        <td class="px-4 py-4 whitespace-nowrap">
                            <div class="text-sm text-gray-900">{{formatPeriod this.fecha_inicio this.fecha_fin}}</div>
                        </td>
                        <td class="px-4 py-4 whitespace-nowrap">
                            <span class="{{this.statusClass}} text-xs font-medium mr-2 px-2.5 py-0.5 rounded-full">
                                {{this.statusText}}
                            </span>
                        </td>
                        <td class="px-4 py-4 whitespace-nowrap">
                            <div class="flex items-center space-x-2">
                                {{#if this.isFamily}} <button
                                    class="view-members-btn action-btn bg-purple-100 text-purple-600 hover:bg-purple-200 hover:scale-110 transition-all duration-200"
                                    title="Ver integrantes" data-id-activa="{{this.id_activa}}">
                                    <i class="fas fa-users"></i>
                                </button>
                                {{/if}}

                                <button
                                    class="view-details-btn action-btn bg-blue-100 text-blue-600 hover:bg-blue-200 hover:scale-110 transition-all duration-200"
                                    title="Ver detalles" data-id="{{this.id_activa}}">
                                    <i class="fas fa-eye"></i>
                                </button>

                                {{#if this.canRenew}}
                                <a href="/memberships/renew/{{this.id_activa}}"
                                    class="action-btn bg-green-100 text-green-600 hover:bg-green-200 hover:scale-110 transition-all duration-200"
                                    title="Renovar">
                                    <i class="fas fa-sync-alt"></i>
                                </a>
                                {{/if}}

                                {{#if ../isAdmin}}
                                <a href="/memberships/editMembership/{{this.id_activa}}"
                                    class="action-btn bg-amber-100 text-amber-600 hover:bg-amber-200 hover:scale-110 transition-all duration-200"
                                    title="Editar">
                                    <i class="fas fa-edit"></i>
                                </a>
                                {{/if}}

                                <button
                                    class="view-history-payments-btn action-btn bg-indigo-100 text-indigo-600 hover:bg-indigo-200 hover:scale-110 transition-all duration-200"
                                    title="Ver historial de pagos" data-id-activa="{{this.id_activa}}">
                                    <i class="fa-solid fa-file-invoice-dollar"></i>
                                </button>

                                {{#if ../isAdmin}}
                                <button type="button"
                                    class="delete-btn action-btn bg-red-100 text-red-600 hover:bg-red-200 hover:scale-110 transition-all duration-200"
                                    action="/memberships/deleteMembership" title="Eliminar" data-id="{{this.id_activa}}"
                                    data-name="{{this.nombre_completo}}"
                                    data-type="{{#if this.isFamily}}Familiar{{else}}Individual{{/if}}">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                                {{/if}}

                            </div>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <!-- Paginación -->
        <div class="px-6 py-4 bg-gray-50 border-t border-gray-200 flex items-center justify-between">
            <div class="text-sm text-gray-700">
                Mostrando <span class="font-medium">{{memberships.length}}</span> de <span
                    class="font-medium">{{estadisticas.total}}</span> resultados
            </div>
            <div class="flex space-x-2">
                <button
                    class="px-3 py-1.5 rounded-lg border-2 border-gray-300 text-gray-600 hover:bg-gray-100 hover:border-gray-400 disabled:opacity-50 disabled:cursor-not-allowed transition-all duration-200">
                    <i class="fas fa-chevron-left"></i>
                </button>
                <button class="px-3 py-1.5 rounded-lg border-2 border-blue-600 bg-blue-600 text-white">
                    1
                </button>
                <button
                    class="px-3 py-1.5 rounded-lg border-2 border-gray-300 text-gray-600 hover:bg-gray-100 hover:border-gray-400 transition-all duration-200">
                    2
                </button>
                <button
                    class="px-3 py-1.5 rounded-lg border-2 border-gray-300 text-gray-600 hover:bg-gray-100 hover:border-gray-400 transition-all duration-200">
                    <i class="fas fa-chevron-right"></i>
                </button>
            </div>
        </div>
    </div>
</div>

{{!-- Modal Genérico para Confirmación/Notificación --}}
<div id="deleteModal" class="fixed inset-0 bg-black bg-opacity-50 items-center flex justify-center z-50 hidden">
    <div class="bg-white rounded-2xl p-6 w-11/12 md:w-1/3 max-w-md transform transition-all duration-300 scale-95">
        <div class="flex items-center mb-4">
            <div id="deleteModalIconContainer" class="p-3 rounded-full mr-4">
                <i id="deleteModalIcon" class="text-xl"></i>
            </div>
            <h3 id="deleteModalTitle" class="text-xl font-semibold text-gray-800"></h3>
        </div>
        <p id="deleteModalMessage" class="text-gray-600 mb-6"></p>
        <div id="deleteModalButtons" class="flex justify-end space-x-3">
            <!-- Los botones se insertarán aquí dinámicamente -->
        </div>
    </div>
</div>

<!-- Templates para Modales -->
<template id="integrantes-modal-template">
    <div
        class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 transition-all duration-300 ease-in-out">
        <div class="bg-white rounded-2xl p-6 w-11/12 md:w-96 max-w-90vw transform transition-all duration-300 scale-95">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">
                <i class="fas fa-users mr-2 text-purple-500"></i>Integrantes de la Membresía
            </h3>
            <div class="max-h-60 overflow-y-auto">
                <table class="min-w-full">
                    <thead>
                        <tr>
                            <th class="text-left text-sm font-medium text-gray-700 py-2">Nombre</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100" data-template-content="integrantes-list">
                        <!-- Los integrantes se insertarán aquí -->
                    </tbody>
                </table>
            </div>
            <div class="mt-4 flex justify-end">
                <button
                    class="px-4 py-2 bg-gradient-to-r from-purple-600 to-indigo-600 text-white rounded-xl hover:shadow-lg transition-all duration-300 transform hover:-translate-y-0.5 close-integrantes-modal">
                    Cerrar
                </button>
            </div>
        </div>
    </div>
</template>

<template id="details-modal-template">
    <div class="fixed inset-0 bg-black bg-opacity-60 flex items-center justify-center z-50">
        <div
            class="bg-white rounded-2xl p-8 w-11/12 md:w-1/2 max-w-2xl max-h-[90vh] overflow-y-auto transform transition-all duration-300 scale-95">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-2xl font-bold text-gray-800">
                    <i class="fas fa-id-card-alt mr-3 text-blue-500"></i>Detalles de la Membresía
                </h3>
                <button class="text-gray-500 hover:text-gray-800 transition-colors duration-300 close-details-modal">
                    <i class="fas fa-times text-2xl"></i>
                </button>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="space-y-4" data-template-content="details-info">
                    <!-- La información de detalles se insertará aquí -->
                </div>
                <div class="flex flex-col items-center justify-center bg-gray-50 p-6 rounded-xl border-2 border-gray-200"
                    data-template-content="details-qr">
                    <!-- El QR se insertará aquí -->
                </div>
            </div>
            <div class="mt-8 flex justify-end">
                <button
                    class="px-5 py-2.5 bg-gray-200 text-gray-700 rounded-xl font-medium hover:bg-gray-300 transition-all duration-300 transform hover:-translate-y-0.5 close-details-modal">
                    Cerrar
                </button>
            </div>
        </div>
    </div>
</template>

<template id="payments-history-modal-template">
    <div
        class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 transition-all duration-300 ease-in-out">
        <div
            class="bg-white rounded-2xl p-6 w-11/12 md:w-[500px] max-w-90vw transform transition-all duration-300 scale-95">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">
                <i class="fas fa-file-invoice-dollar mr-2 text-indigo-500"></i>Historial de Pagos
            </h3>
            <div class="max-h-80 overflow-y-auto">
                <table class="min-w-full">
                    <thead>
                        <tr class="border-b">
                            <th class="text-left text-sm font-medium text-gray-700 py-2">Fecha</th>
                            <th class="text-left text-sm font-medium text-gray-700 py-2">Método</th>
                            <th class="text-right text-sm font-medium text-gray-700 py-2">Monto</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100" data-template-content="payments-list">
                        <!-- Los pagos se insertarán aquí -->
                    </tbody>
                </table>
            </div>
            <div class="mt-4 flex justify-end">
                <button
                    class="px-4 py-2 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-xl hover:shadow-lg transition-all duration-300 transform hover:-translate-y-0.5 close-payments-modal">Cerrar</button>
            </div>
        </div>
    </div>
</template>

<script src="/js/moduleMembershipJs/listMembership.js"></script>
<script src="/js/moduleMembershipJs/deleteMembership.js"></script>