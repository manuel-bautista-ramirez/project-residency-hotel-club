<link rel="stylesheet" href="/public/css/css/all.min.css">
<link rel="stylesheet" href="/public/css/css/v5-font-face.css">
<link rel="stylesheet" href="/public/css/css/v5-shims.css">

<body class="bg-gray-50 min-h-screen">
    <!-- Navbar -->
    {{> navbar}}

    <div class="container mx-auto px-4 py-6 mt-20">
        <!-- Header -->
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">Renovar Membresía</h1>
                <p class="text-gray-600 mt-2">Renovando la membresía #{{membership.id_activa}}</p>
            </div>

            <div class="flex flex-wrap gap-3 mt-4 md:mt-0">
                <a href="/memberships"
                    class="bg-gray-600 hover:bg-gray-700 text-white px-5 py-2.5 rounded-lg font-medium flex items-center transition-colors">
                    <i class="fas fa-arrow-left mr-2"></i> Volver
                </a>
            </div>
        </div>

        <!-- Formulario de renovación -->
        <form id="renewMembershipForm" action="/memberships/renew/{{membership.id_activa}}" method="POST"
            class="bg-white rounded-2xl shadow-lg p-6 mb-8 border border-green-100">
            <input type="hidden" id="id_activa" name="id_activa" value="{{membership.id_activa}}">
            <input type="hidden" name="id_cliente" value="{{membership.id_cliente}}">


            <!-- Información del cliente -->
            <div class="mb-8">
                <h2 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-user-circle mr-2 text-green-500"></i> Información del Cliente
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="nombre_completo">
                            Nombre Completo *
                        </label>
                        <input type="text" id="nombre_completo" name="nombre_completo"
                            value="{{membership.nombre_completo}}"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent"
                            required>
                    </div>

                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="telefono">
                            Teléfono
                        </label>
                        <input type="tel" id="telefono" name="telefono" value="{{membership.telefono}}"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent">
                    </div>

                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="correo">
                            Correo Electrónico
                        </label>
                        <input type="email" id="correo" name="correo" value="{{membership.correo}}"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent">
                    </div>
                </div>
            </div>

            <!-- Información de la membresía -->
            <div class="mb-8">
                <h2 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-id-card-alt mr-2 text-green-500"></i> Información de la Membresía
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="id_tipo_membresia">
                            Tipo de Membresía *
                        </label>
                        <select id="id_tipo_membresia" name="id_tipo_membresia"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent"
                            required>
                            {{#each tiposMembresia}}
                            <option value="{{this.id_tipo_membresia}}" {{#if (eq ../membership.tipo_membresia this.nombre)}}selected{{/if}}>
                                {{this.nombre}} - ${{this.precio}}
                            </option>
                            {{/each}}
                        </select>
                    </div>

                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="fecha_inicio">
                            Fecha de Inicio *
                        </label>
                        <input type="date" id="fecha_inicio" name="fecha_inicio"
                            value="{{formatDate (now)}}"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg bg-gray-100"
                            readonly>
                    </div>

                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="fecha_fin">
                            Fecha de Vencimiento
                        </label>
                        <input type="date" id="fecha_fin" name="fecha_fin"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg bg-gray-100"
                            readonly>
                    </div>
                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="id_metodo_pago">
                            Método de pago *
                        </label>
                        <select id="id_metodo_pago" name="id_metodo_pago"
                            class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent"
                            required>
                            {{#each tiposPago}}
                            <option value="{{this.id_metodo_pago}}">
                                {{this.nombre}}
                            </option>
                            {{/each}}
                        </select>
                    </div>
                </div>
            </div>

            <!-- Botones de acción -->
            <div class="flex justify-end space-x-3 pt-6 border-t border-gray-200">
                <a href="/memberships"
                    class="inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500">
                    <i class="fas fa-times mr-2"></i> Cancelar
                </a>
                <button type="submit"
                    class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500">
                    <i class="fas fa-sync-alt mr-2"></i> Renovar Membresía
                </button>
            </div>
        </form>
    </div>

    <script src="/js/moduleMembershipJs/renewal.js"></script>
</body>
