

  <!-- HEADER que contiene:
       - En desktop: logo + enlaces en línea
       - En móvil: logo + botón hamburguesa -->
  {{>navbar}}

  <!-- Overlay semitransparente cuando sidebar está abierto (solo en móvil) -->
  <div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 hidden sm:hidden z-30"></div>

  <!-- Contenido principal -->
  <main class="pt-16 px-4 sm:px-8 max-w-7xl mx-auto">
    <!-- Padding top (pt-16) para que no quede bajo el header fijo -->
    <!-- Optional: header interno en desktop -->
    <section class="hidden sm:block text-center mb-8">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-blue-800 drop-shadow-md">
        Bienvenido al Hotel Residencial
      </h1>
      <p class="text-base sm:text-lg md:text-xl text-gray-700 mt-2">
        Hola,
        {{username}}. Tu rol es:
        <strong>{{role}}</strong>.
      </p>
    </section>

    <!-- Mensajes por rol -->
    <section class="text-center mb-8">
      <ul class="space-y-2">
        {{#if (eq role "Administrador")}}
        <li>
          <p class="text-sm text-gray-600">Como Administrador, tienes
            acceso completo al sistema.</p>
        </li>
        {{/if}}
        {{#if (eq role "Usuario")}}
        <li>
          <p class="text-sm text-gray-600">Como Recepcionista, puedes
            gestionar reservas y atender a los clientes.</p>
        </li>
        {{/if}}
      </ul>
    </section>

    <!-- Resto del contenido -->
    <section>
      <h2 class="text-2xl font-semibold text-blue-700 mb-4">Sección de ejemplo</h2>
      <p class="text-gray-700">
        Aquí va el contenido principal de la página. Ajusta según tus
        necesidades.
      </p>
    </section>
  </main>

