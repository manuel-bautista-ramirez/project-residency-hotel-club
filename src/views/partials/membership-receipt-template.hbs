<div class="p-8 bg-white text-gray-800" style="font-family: Arial, sans-serif; max-width: 800px; margin: auto;">

  <!-- Encabezado -->
  <div class="text-center mb-8">
    <h1 class="text-3xl font-bold text-green-600">Hotel Club</h1>
    <h2 class="text-xl text-gray-600">Comprobante de Membresía</h2>
  </div>

  <!-- Información de la Membresía -->
  <div class="border-2 border-green-600 rounded-lg p-6 mb-6">
    <h3 class="text-lg font-semibold text-green-600 mb-4 border-b pb-2">Información de la Membresía</h3>
    <div class="grid grid-cols-2 gap-x-8 gap-y-4">
      <p><strong>Titular:</strong> {{titularNombre}}</p>
      <p><strong>Tipo de membresía:</strong> {{tipoMembresia}}</p>
      <p><strong>Fecha de inicio:</strong> {{fechaInicio}}</p>
      <p><strong>Fecha de expiración:</strong> {{fechaFin}}</p>
      <p><strong>Método de pago:</strong> {{metodoPago}}</p>
      <p><strong>Total pagado:</strong> {{precioFinal}} MXN</p>
      <p class="col-span-2"><strong>Total en letras:</strong> {{precioEnLetras}}</p>
    </div>
  </div>

  <!-- Integrantes (si existen) -->
  {{#if integrantes}}
  <div class="border-2 border-gray-300 rounded-lg p-6 mb-6">
    <h3 class="text-lg font-semibold text-gray-700 mb-4 border-b pb-2">Integrantes de la Membresía</h3>
    <ul class="list-disc list-inside">
      {{#each integrantes}}
      <li>{{this.nombre_completo}}</li>
      {{/each}}
    </ul>
  </div>
  {{/if}}

  <!-- Código QR (si está disponible) -->
  {{#if qrBase64}}
  <div class="text-center mb-6">
    <div class="inline-block border-2 border-gray-100 p-2 rounded-xl">
      <img src="{{qrBase64}}" alt="Código QR de Acceso" style="width: 150px; height: 150px;">
    </div>
    <p class="text-[10px] text-gray-400 mt-2 uppercase tracking-widest font-black">Tu Código de Acceso Digital</p>
  </div>
  {{/if}}

  <!-- Mensaje de bienvenida -->
  <div class="bg-green-50 border border-green-200 rounded-lg p-4 text-center text-green-800">
    <p><strong>¡Bienvenido a Hotel Club!</strong></p>
    <p class="text-sm">Su membresía ha sido activada exitosamente. Pregunte por su código QR en recepción.</p>
  </div>

  <!-- Pie de página -->
  <div class="text-center mt-8 text-xs text-gray-500">
    <p>Este es un comprobante automático generado por el sistema.</p>
    <p>Para cualquier consulta, contacte con nosotros.</p>
  </div>

</div>
