<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>
  <link href="/styles.css" rel="stylesheet" />
</head>
<body class="bg-gray-100 {{#if centerContent}}min-h-screen flex justify-center items-center{{/if}}">

  {{{body}}}

  {{#if showFooter}}
    {{> footer year=(year)}}
  {{/if}}
</body>
</html>
